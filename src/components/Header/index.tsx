import React from 'react';

import { HeaderContent } from './styles';

const Header: React.FC = () => {
  return (
    <HeaderContent>
      <main>
        <h2>Todo Luby</h2>
      </main>
    </HeaderContent>
  );
};

export default Header;
